# 本地开发：走 Vite 代理，避免跨域（见 vite.config.js）
VITE_API_BASE_URL=/api
# 留空则 /uploads 同样走 Vite 代理到本机 API
VITE_ASSET_BASE_URL=
# 可选：API 不在 3000 端口时覆盖代理目标，如 http://127.0.0.1:3001
# VITE_DEV_API_TARGET=http://127.0.0.1:3000
