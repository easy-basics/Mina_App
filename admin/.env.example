# 复制为 .env.development.local 可覆盖本地配置（勿提交敏感信息）
# 开发默认见 .env.development，生产默认见 .env.production

VITE_API_BASE_URL=http://localhost:3000/api
# 图片 CDN 根地址（可选；本地连线上库时可设为 https://api.mina.bigdeng.com）
# VITE_ASSET_BASE_URL=https://api.mina.bigdeng.com
# 图片展示由 utils/media.js 根据上述变量拼接 /uploads 绝对路径
