# 复制为 .env.development.local 可覆盖本地配置（勿提交敏感信息）
# 开发默认见 .env.development，生产默认见 .env.production

VITE_API_BASE_URL=/api
# 开发时由 Vite 代理 /api、/uploads 到本机 API（默认 http://127.0.0.1:3000）
# 图片展示：/uploads 使用相对路径，由 Vite 或 nginx 代理到 API
